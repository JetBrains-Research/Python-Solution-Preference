# Pilot's Logbook (MVP)

Single-user pilot logbook exposed as an HTTP API (no UI). All dates/times are in Zulu (UTC). Data is stored in memory.

## Requirements

- Python 3.10+
- Flask 3.x (`pip install flask`)

## Running
